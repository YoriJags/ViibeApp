---
phase: 03-push-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routes/venue_live.py
  - backend/app/services/vibe.py
autonomous: true
requirements:
  - NOTF-03
  - NOTF-04
must_haves:
  truths:
    - "Merchant live blast delivers to scouts within 2km of the venue, not just followers"
    - "When a venue vibe score drops below 50, the merchant receives a push notification automatically — no Aura Shield configuration required"
  artifacts:
    - path: "backend/app/routes/venue_live.py"
      provides: "POST /api/venues/:id/live-push with geo-query for nearby scouts"
      contains: "2000"
    - path: "backend/app/services/vibe.py"
      provides: "_check_score_drop_alert() called after every score update"
      contains: "score < 50"
  key_links:
    - from: "backend/app/routes/venue_live.py"
      to: "db.alert_preferences"
      via: "geo-query on db.users then token lookup"
      pattern: "nearby.*scouts"
    - from: "backend/app/services/vibe.py"
      to: "notify_merchant_vibe_alert"
      via: "_check_score_drop_alert called from calculate_venue_aggregate"
      pattern: "_check_score_drop_alert"
---

<objective>
Wire the two missing push-delivery triggers: geo-targeted live blasts (NOTF-03) and automatic vibe-score-drop alerts to merchants (NOTF-04).

Purpose:
- Live blast currently only reaches venue followers. Scouts within 2km who don't follow the venue miss it entirely — defeating the "city's energy visible in real-time" pitch.
- Score-below-50 merchant alert only fires when merchant has manually configured an Aura Shield. Most merchants won't configure it. NOTF-04 requires it to be automatic.

Output: Live blast reaches nearby scouts within 2km + followers (deduplicated). Merchant gets a push when their venue score crosses below 50, always, with a 1-hour cooldown to prevent spam.
</objective>

<execution_context>
@C:/Users/OAJAGUN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OAJAGUN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/VIIBE/VibeApp/.planning/ROADMAP.md

Key existing code:
- backend/app/routes/venue_live.py — POST /api/venues/:id/live-push (line 54-133). Currently queries db.venue_follows for followers, sends push to each. No geo-query.
- backend/app/services/vibe.py — _check_aura_shield() at line 1070 fires merchant alert only if Aura Shield is configured. calculate_venue_aggregate() calls _check_aura_shield() at line 1067.
- backend/app/services/notifications.py — notify_merchant_vibe_alert(venue_id, score, reason) at line 109 — reads owner_id from venue, reads token from db.alert_preferences.
- db.users has location field with lat/lng for each user (scouts store location from Expo Location).
- db.alert_preferences has expo_push_token per user_id.

Venue document structure (from db.venues):
  id, name, owner_id, category, location: {lat, lng} or coordinates: [lng, lat]
  current_vibe_score: float
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add geo-query for nearby scouts to live blast endpoint</name>
  <files>backend/app/routes/venue_live.py</files>
  <action>
    In the send_live_push() handler (POST /api/venues/:id/live-push), after sending to followers, add a geo-query for scouts within 2km who have NOT already been notified (dedup with follower_ids set).

    Implementation steps:
    1. Get venue lat/lng: venue_lat = venue.get("location", {}).get("lat") or venue.get("lat"); venue_lng = venue.get("location", {}).get("lng") or venue.get("lng"). If neither exists, skip geo step gracefully.
    2. Query nearby users using Haversine distance approximation (MongoDB $geoNear requires 2dsphere index; to avoid requiring index migration, use a bounding box approximation):
       - 2km in degrees lat ≈ 0.018; lng ≈ 0.018 / cos(lat * pi/180)
       - Query: db.users.find({"location.lat": {"$gte": lat-0.018, "$lte": lat+0.018}, "location.lng": {"$gte": lng-0.018, "$lte": lng+0.018}}, {"id": 1}).to_list(500)
       - This over-queries slightly (corners of bounding box), which is acceptable for push notifications.
    3. Collect nearby user IDs, subtract follower_ids to avoid duplicate pushes.
    4. For each nearby non-follower, fetch their push token from db.alert_preferences, check nearby_alerts preference is True (default True), send push with same title/body/data as followers get.
    5. Add nearby_sent count to response: return {..."nearby_sent": nearby_sent, "notifications_sent": sent + nearby_sent}

    Keep the existing follower notification loop intact — just add nearby scouts after it.

    Cap total recipients: if follower_ids + nearby_ids > 2000, truncate nearby_ids to 2000 - len(follower_ids).
  </action>
  <verify>
    <automated>cd C:/VIIBE/VibeApp/backend && python -m py_compile app/routes/venue_live.py && echo "syntax OK"</automated>
  </verify>
  <done>Live blast endpoint sends to followers + nearby scouts within 2km (deduplicated). Response includes nearby_sent field. Syntax valid.</done>
</task>

<task type="auto">
  <name>Task 2: Add automatic vibe-score-drop-below-50 merchant alert</name>
  <files>backend/app/services/vibe.py</files>
  <action>
    Add a new async function _check_score_drop_alert(venue_id: str, aggregate: dict, previous_score: float | None) in vibe.py.

    Logic:
    - current_score = aggregate.get("current_vibe_score", 100)
    - THRESHOLD = 50
    - Only fire if current_score < THRESHOLD AND (previous_score is None or previous_score >= THRESHOLD)
      This is a crossing-threshold check — only fires when score crosses below 50, not on every score update below 50.
    - Cooldown: check db.merchant_score_alerts for a doc {"venue_id": venue_id, "alert_type": "score_below_50"}. If last_triggered exists and is within 60 minutes, skip.
    - If firing: upsert db.merchant_score_alerts {"venue_id": venue_id, "alert_type": "score_below_50", "last_triggered": now}
    - Then call: from app.services.notifications import notify_merchant_vibe_alert; await notify_merchant_vibe_alert(venue_id, current_score, f"Vibe score dropped below 50 ({current_score:.0f}%) — consider sending a live blast to re-energise the crowd.")

    In calculate_venue_aggregate() (or wherever scores are saved to the venue document — around line 589-595):
    - Read previous_score = venue.get("current_vibe_score") BEFORE the update (this is likely already done — check the existing code)
    - After saving the new score, call: await _check_score_drop_alert(venue_id, aggregate, previous_score)

    Also keep the existing _check_aura_shield() call — these are complementary, not replacements.

    Import needed: from datetime import datetime, timezone, timedelta at top of file (likely already imported).
  </action>
  <verify>
    <automated>cd C:/VIIBE/VibeApp/backend && python -m py_compile app/services/vibe.py && echo "syntax OK"</automated>
  </verify>
  <done>_check_score_drop_alert() exists in vibe.py. It fires only on threshold crossing (not every update). 60-minute cooldown using db.merchant_score_alerts. Called from calculate_venue_aggregate. Syntax valid.</done>
</task>

</tasks>

<verification>
1. Syntax: `cd C:/VIIBE/VibeApp/backend && python -m py_compile app/routes/venue_live.py app/services/vibe.py`
2. Live blast response has nearby_sent field: `grep "nearby_sent" C:/VIIBE/VibeApp/backend/app/routes/venue_live.py`
3. Score alert function exists: `grep "_check_score_drop_alert" C:/VIIBE/VibeApp/backend/app/services/vibe.py`
4. Score alert called from aggregate function: `grep -A2 "_check_score_drop_alert" C:/VIIBE/VibeApp/backend/app/services/vibe.py | grep "await"`
</verification>

<success_criteria>
- Live blast reaches nearby scouts (bounding-box 2km) + followers, deduplicated, capped at 2000
- Merchant push fires automatically when score crosses below 50, with 60-min cooldown, without requiring Aura Shield configuration
- Both vibe.py and venue_live.py compile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-push-notifications/03-02-SUMMARY.md`
</output>
