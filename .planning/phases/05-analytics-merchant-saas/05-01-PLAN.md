---
phase: 05-analytics-merchant-saas
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routes/merchant.py
  - frontend/src/components/VenueAnalyticsCard.tsx
autonomous: true
requirements:
  - ANAL-01

must_haves:
  truths:
    - "Merchant analytics API returns avg_score_7d, avg_score_30d, rating_volume_by_hour, peak_day_of_week, and unique_scout_count in a single response"
    - "API response is cached in MongoDB (merchant_analytics_cache collection) and returns stale data if cache is fresh (< 2 hours old)"
    - "VenueAnalyticsCard renders 7-day and 30-day score trend numbers, a bar showing peak hour, the peak day of week, and unique scout count"
    - "Analytics card loads in under 2 seconds by reading from cache"
  artifacts:
    - path: "backend/app/routes/merchant.py"
      provides: "GET /merchant/venues/{venue_id}/analytics with cache + 7d metrics"
      contains: "avg_score_7d"
    - path: "frontend/src/components/VenueAnalyticsCard.tsx"
      provides: "Renders the new 7d/30d trend, peak day, unique scout count"
      contains: "avg_score_7d"
  key_links:
    - from: "frontend/src/components/VenueAnalyticsCard.tsx"
      to: "/merchant/venues/{venue_id}/analytics"
      via: "fetch on mount"
      pattern: "merchant/venues.*analytics"
---

<objective>
Wire up the merchant analytics data product so a paid SaaS merchant sees pre-computed intelligence that justifies the ₦150,000/month subscription.

Purpose: ANAL-01 — Merchant dashboard shows pre-computed analytics loading in under 2 seconds.
Output: Updated API endpoint with 7-day metrics + caching, updated VenueAnalyticsCard rendering the full data set.
</objective>

<execution_context>
@C:/Users/OAJAGUN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OAJAGUN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

The existing `/merchant/venues/{venue_id}/analytics` route in `backend/app/routes/merchant.py` (line 710) already computes:
- 30-day daily vibe history
- Peak hours (last 30 days)
- Day-of-week breakdown
- Slow nights
- Area rank
- Top 5 scouts (last 7 days)
- summary.avg_score_30d, total_ratings_30d, best_day, worst_day, trend

What is MISSING from the ANAL-01 spec:
- `avg_score_7d` — 7-day average vibe score (separate from 30d)
- `unique_scout_count` — total distinct user_ids who rated this venue in 30 days
- `peak_day_of_week` — the single highest-rated day name (already derivable from day_breakdown)
- **Caching** — no cache exists; every request re-queries. Must cache to MongoDB `merchant_analytics_cache` with 2-hour TTL.

VenueAnalyticsCard (`frontend/src/components/VenueAnalyticsCard.tsx`) already renders vibe_history, peak_hours, day_breakdown, area_rank, top_scouts and summary — but does NOT show avg_score_7d or unique_scout_count.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 7d avg, unique scout count, peak day, and 2-hour cache to merchant analytics API</name>
  <files>backend/app/routes/merchant.py</files>
  <action>
In `get_venue_analytics` (line 710 in merchant.py):

1. **Cache check** — At the top of the function (after venue lookup), query `merchant_analytics_cache` for a document where `venue_id == venue_id` and `cached_at >= now - 2 hours`. If found, return `doc["data"]` immediately.

2. **Add avg_score_7d** — After the existing `ratings_30d` query, filter for records where `timestamp >= seven_days_ago` (already defined at line 721) to get `ratings_7d`. Compute `avg_score_7d = round(sum(r["vibe_score"] for r in ratings_7d) / len(ratings_7d)) if ratings_7d else 0`.

3. **Add unique_scout_count** — After computing `ratings_30d`, derive `unique_scout_count = len(set(r.get("user_id") for r in ratings_30d if r.get("user_id")))`. Note: the ratings documents need `user_id` projected — update the projection in the `ratings_30d` query from `{"vibe_score": 1, "timestamp": 1, "_id": 0}` to also include `"user_id": 1`.

4. **Add peak_day_of_week** — Derive from existing `day_breakdown` list: `peak_day = max(day_breakdown, key=lambda d: d["avg_score"], default=None)`. Add `"peak_day_of_week": peak_day["day"] if peak_day else None` to the return dict. This can reuse existing `best_day` logic — just expose it at top level.

5. **Add new fields to return dict summary**: add `"avg_score_7d": avg_score_7d` and `"unique_scout_count": unique_scout_count` to `summary` dict.

6. **Cache write** — Before returning the response dict, upsert into `merchant_analytics_cache`:
   ```python
   await db.merchant_analytics_cache.update_one(
       {"venue_id": venue_id},
       {"$set": {"venue_id": venue_id, "data": result, "cached_at": now}},
       upsert=True,
   )
   ```
   Then return `result`.

Do NOT refactor unrelated code. Only touch the `get_venue_analytics` function.
  </action>
  <verify>
    <automated>cd /c/VIIBE/VibeApp/backend && python -c "import ast, sys; ast.parse(open('app/routes/merchant.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
    - `GET /merchant/venues/{id}/analytics` returns JSON with `summary.avg_score_7d`, `summary.unique_scout_count`, and `peak_day_of_week` at top level
    - Second call to same endpoint within 2 hours returns cached result (verify by checking `merchant_analytics_cache` collection has a document for the venue)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update VenueAnalyticsCard to display 7-day trend, unique scout count, and peak day</name>
  <files>frontend/src/components/VenueAnalyticsCard.tsx</files>
  <action>
In `VenueAnalyticsCard.tsx`:

1. **Extend the Analytics interface** — Add to the `summary` sub-interface:
   ```typescript
   avg_score_7d: number;
   unique_scout_count: number;
   ```
   Add `peak_day_of_week: string | null` at the top-level `Analytics` interface.

2. **Add a "Score Trend" stats row** — In the rendered JSX, below the existing summary `trend` badge and before the vibe history chart, insert a horizontal row of 3 stat pills:
   - "7-day avg" showing `analytics.summary.avg_score_7d`
   - "30-day avg" showing `analytics.summary.avg_score_30d`
   - "Unique scouts" showing `analytics.summary.unique_scout_count`

   Use the Emergent design tokens from `../../src/theme/floors` (already imported or accessible via the merchantTheme pattern used in the merchant index screen). Match the dark card style: `backgroundColor: '#1A1A2E'`, border `#2D2D4E`, text `#FFFFFF` for values, `#94A3B8` for labels, `borderRadius: 12`, `padding: 12`.

3. **Add peak day display** — If `analytics.peak_day_of_week` is non-null, show a single line below the stat row: "Peak day: {peak_day_of_week}" with a calendar icon (use `Ionicons` `calendar-outline`). Style in gold (`#FFD700`) to make it scannable.

Do NOT remove any existing rendered sections. Only add the new stat row and peak day line.
  </action>
  <verify>
    <automated>cd /c/VIIBE/VibeApp/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - TypeScript compiles without errors in VenueAnalyticsCard.tsx
    - Component interface includes avg_score_7d, unique_scout_count, peak_day_of_week
    - Three stat pills render when data is present
  </done>
</task>

</tasks>

<verification>
- `python -c "import ast; ast.parse(open('backend/app/routes/merchant.py').read()); print('OK')"` passes
- `npx tsc --noEmit` passes with no errors in VenueAnalyticsCard.tsx
- Response from `/merchant/venues/{id}/analytics` includes `summary.avg_score_7d`, `summary.unique_scout_count`, `peak_day_of_week`
</verification>

<success_criteria>
Merchant analytics API returns the full ANAL-01 data set (7-day avg, 30-day avg, rating volume by hour, peak day of week, unique scout count) with a 2-hour MongoDB cache. VenueAnalyticsCard renders these values. A second API call within 2 hours hits the cache and returns in well under 2 seconds.
</success_criteria>

<output>
After completion, create `.planning/phases/05-analytics-merchant-saas/05-01-SUMMARY.md`
</output>
