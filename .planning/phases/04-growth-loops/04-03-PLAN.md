---
phase: 04-growth-loops
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/VibeMomentCard.tsx
  - frontend/app/venue/[id].tsx
autonomous: true
requirements:
  - GROW-03

must_haves:
  truths:
    - "A scout can share a Vibe Moment card image directly to WhatsApp or Instagram Stories from the venue detail screen"
    - "The card contains the venue name, live vibe score, and scout handle"
    - "The share sheet appears natively — scout picks WhatsApp or Instagram from the OS share picker"
  artifacts:
    - path: "frontend/src/components/VibeMomentCard.tsx"
      provides: "Renderable card component captured as image via react-native-view-shot"
      exports: ["VibeMomentCard"]
    - path: "frontend/app/venue/[id].tsx"
      provides: "Share button wiring: capture card + call expo-sharing"
  key_links:
    - from: "VibeMomentCard (React Native view)"
      to: "react-native-view-shot captureRef()"
      via: "ref passed to ViewShot wrapper, captured as PNG"
      pattern: "captureRef|ViewShot"
    - from: "captured PNG URI"
      to: "expo-sharing shareAsync()"
      via: "direct call with mimeType image/png"
      pattern: "shareAsync"
---

<objective>
Build the Vibe Moment share card: a styled React Native view captured as a PNG image and shared natively to WhatsApp or Instagram Stories via the OS share sheet.

Purpose: GROW-03 — scouts become VIIBE ambassadors by sharing live venue energy to their social networks.
Output: VibeMomentCard component + share button on venue detail screen. Two files modified.
</objective>

<execution_context>
@C:/Users/OAJAGUN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OAJAGUN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@frontend/app/venue/[id].tsx
@frontend/src/components/MemoryArtifact.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build VibeMomentCard component</name>
  <files>frontend/src/components/VibeMomentCard.tsx</files>
  <action>
    Create frontend/src/components/VibeMomentCard.tsx — a pure React Native view (no Skia, no Reanimated) designed to be captured as a PNG.

    Props interface:
    ```typescript
    interface VibeMomentCardProps {
      venueName: string;
      area: string;          // e.g. "Victoria Island"
      score: number;         // 0-100
      scoutHandle: string;   // e.g. "@lagos_scout"
      captureRef: React.RefObject<View>;
    }
    ```

    Visual design (Emergent luxury aesthetic):
    - Background: #08070F (near-black)
    - Outer border: 1px solid rgba(201,168,76,0.3) (gold, subtle)
    - Dimensions: fixed 320×180 (16:9, fits WhatsApp preview)
    - Score display: large numeral (48px) in color mapped from score:
      - 80+: #00F0FF (cyan, PEAK)
      - 60-79: #C9A84C (gold, ELECTRIC)
      - <60: #E8E4DA (warm white, WARMING)
    - Score label: state word (PEAK / ELECTRIC / WARMING) in 11px uppercase tracked letters, same color as score
    - Venue name: 18px semibold, #E8E4DA, max 2 lines
    - Area: 12px, rgba(232,228,218,0.5), 1 line
    - Scout handle: bottom-right, 11px, rgba(201,168,76,0.7)
    - VIIBE wordmark: bottom-left, 11px uppercase tracked, rgba(232,228,218,0.3)
    - Horizontal rule between score and venue name: 1px rgba(201,168,76,0.15)

    Use only React Native core (View, Text, StyleSheet) — no third-party UI. The `captureRef` prop is passed as the `ref` on the outermost View.

    Export as named export `VibeMomentCard` and as default.
  </action>
  <verify>
    <automated>cd c:/VIIBE/VibeApp/frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep "VibeMomentCard" | grep -i error | head -5; echo "VibeMomentCard TS check done"</automated>
  </verify>
  <done>VibeMomentCard.tsx compiles without TS errors. Component accepts all props and renders a 320x180 styled card.</done>
</task>

<task type="auto">
  <name>Task 2: Wire share button on venue detail screen</name>
  <files>frontend/app/venue/[id].tsx</files>
  <action>
    In frontend/app/venue/[id].tsx, add a "Share Vibe" button that:

    1. Creates a ref: `const cardRef = useRef<View>(null)`
    2. Renders VibeMomentCard off-screen (position absolute, opacity 0, pointerEvents none) with cardRef, passing:
       - venueName from existing venue data
       - area from existing venue data
       - score from existing live score state
       - scoutHandle: current user's username prefixed with "@" (read from auth context or user state already present in the screen)
    3. On button tap:
       a. `const uri = await captureRef(cardRef, { format: 'png', quality: 1.0 })`  — import captureRef from 'react-native-view-shot'
       b. `await shareAsync(uri, { mimeType: 'image/png', dialogTitle: 'Share your Vibe Moment' })` — import shareAsync from 'expo-sharing'
       c. Wrap in try/catch; on error log to console and show a brief inline error text

    Button placement: Add "Share Vibe" as an icon-text button in the venue detail action bar alongside any existing action buttons (check-in, rate, etc.). Use a share icon. Style consistently with existing action buttons on the screen.

    Loading state: disable button and show spinner while capture/share is in progress.

    Import additions only — do not restructure the screen. The off-screen card render adds one View to the JSX tree; everything else is additive.

    Note: react-native-view-shot 4.0.3 and expo-sharing ~14.0.8 are already in package.json — no install needed.
  </action>
  <verify>
    <automated>cd c:/VIIBE/VibeApp/frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep "venue/\[id\]" | grep -i error | head -10; echo "venue screen TS check done"</automated>
  </verify>
  <done>venue/[id].tsx compiles cleanly. VibeMomentCard is rendered off-screen. Share button calls captureRef then shareAsync. Native share sheet appears on tap.</done>
</task>

</tasks>

<verification>
- VibeMomentCard.tsx exists with correct props interface
- venue/[id].tsx imports VibeMomentCard, captureRef, and shareAsync
- TypeScript compiles without errors on both files
- Off-screen card is positioned outside visible viewport (absolute + opacity 0)
</verification>

<success_criteria>
A scout taps "Share Vibe" on a venue detail screen. The OS share sheet appears with a PNG image of the Vibe Moment card (venue name, score, their handle). They select WhatsApp or Instagram and the card is sent.
</success_criteria>

<output>
After completion, create `.planning/phases/04-growth-loops/04-03-SUMMARY.md`
</output>
