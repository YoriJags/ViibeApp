---
phase: 04-growth-loops
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - frontend/app.json
  - frontend/app/_layout.tsx
  - frontend/app/invite/[code].tsx
  - frontend/src/hooks/useReferral.ts
  - frontend/app/(public)/profile.tsx
autonomous: true
requirements:
  - GROW-01
  - GROW-02

must_haves:
  truths:
    - "Scout can tap Invite on their profile, see their referral link, and share it to WhatsApp in two taps"
    - "Tapping viibe.app/invite/:code with VIIBE installed opens the app to the invite screen"
    - "Tapping viibe.app/invite/:code without VIIBE installed goes to App Store / Play Store"
    - "The referral code survives through install attribution (stored in AsyncStorage on first link open)"
    - "When a new user completes signup after using a referral link, POST /referrals/redeem is called automatically"
  artifacts:
    - path: "frontend/app/invite/[code].tsx"
      provides: "Deep link landing screen that resolves code and triggers redemption"
    - path: "frontend/src/hooks/useReferral.ts"
      provides: "Hook that reads pending referral code from AsyncStorage and calls redeem API"
    - path: "frontend/app/(public)/profile.tsx"
      provides: "Invite button that fetches referral link and opens WhatsApp share sheet"
  key_links:
    - from: "viibe.app/invite/:code URL"
      to: "frontend/app/invite/[code].tsx"
      via: "Expo Linking + app.json intentFilters / associatedDomains"
      pattern: "intentFilters|associatedDomains"
    - from: "frontend/src/hooks/useReferral.ts"
      to: "POST /referrals/redeem"
      via: "Called from auth completion screen after signup"
      pattern: "referrals/redeem"
---

<objective>
Wire deep links and the invite UI: configure universal links so viibe.app/invite/:code opens the app, build the invite button on the profile screen, and auto-redeem the referral code when a new user completes signup.

Purpose: GROW-02 (deep link attribution) and the invite UI half of GROW-01.
Output: Working deep link config, invite button with WhatsApp share, and referral code auto-redemption on signup completion.
</objective>

<execution_context>
@C:/Users/OAJAGUN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OAJAGUN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-growth-loops/04-01-SUMMARY.md

@frontend/app.json
@frontend/app/_layout.tsx
@frontend/app/(public)/profile.tsx
</context>

<interfaces>
From backend/app/routes/referrals.py (created in 04-01):

GET /referrals/my-code → { code: string, link: string }
POST /referrals/redeem { code: string } → { message: string, clout_awarded: number, referrer_clout_awarded: number }
Errors: 400 (already redeemed / self-referral), 401 (unauth), 404 (code not found)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Configure universal links and deep link scheme in app.json</name>
  <files>frontend/app.json</files>
  <action>
    In frontend/app.json, add deep link configuration so viibe.app/invite/:code opens the app:

    Under `expo.ios`, add:
    ```json
    "associatedDomains": ["applinks:viibe.app"]
    ```

    Under `expo.android`, add:
    ```json
    "intentFilters": [
      {
        "action": "VIEW",
        "autoVerify": true,
        "data": [
          {
            "scheme": "https",
            "host": "viibe.app",
            "pathPrefix": "/invite"
          }
        ],
        "category": ["BROWSABLE", "DEFAULT"]
      }
    ]
    ```

    The existing `"scheme": "viibe"` custom scheme already handles viibe:// deep links — leave it unchanged.

    NOTE: Universal links (HTTPS) require a hosted `apple-app-site-association` file at https://viibe.app/.well-known/apple-app-site-association. Add a code comment in app.json (as a `_comment` field) noting: "apple-app-site-association must be deployed to viibe.app before iOS universal links work — see Phase 2 EAS Build tasks". For Android autoVerify, a `assetlinks.json` must be at https://viibe.app/.well-known/assetlinks.json — same comment. The app will fall back to the `viibe://` custom scheme from WhatsApp shares until those files are deployed.
  </action>
  <verify>
    <automated>cd c:/VIIBE/VibeApp/frontend && node -e "const a = require('./app.json'); const ios = a.expo.ios; const android = a.expo.android; console.assert(ios.associatedDomains && ios.associatedDomains.includes('applinks:viibe.app'), 'iOS missing'); console.assert(android.intentFilters && android.intentFilters.length > 0, 'Android missing'); console.log('app.json deep link config OK')"</automated>
  </verify>
  <done>app.json contains associatedDomains for iOS and intentFilters for Android targeting viibe.app/invite paths.</done>
</task>

<task type="auto">
  <name>Task 2: Build invite deep link screen and referral hook</name>
  <files>frontend/app/invite/[code].tsx, frontend/src/hooks/useReferral.ts</files>
  <action>
    Create frontend/src/hooks/useReferral.ts:
    - Export `storePendingReferral(code: string)`: saves code to AsyncStorage under key `"pending_referral_code"`
    - Export `redeemPendingReferral(authToken: string)`: reads code from AsyncStorage; if present, calls POST /referrals/redeem with Bearer token; on success OR on 400 "Already redeemed" response, removes the key from AsyncStorage (so it is only attempted once); on 404/other errors, also removes the key (don't retry bad codes); returns the API response or null
    - Export `getPendingReferral()`: returns stored code or null (for display purposes)

    Create frontend/app/invite/[code].tsx:
    - This screen is the deep link landing target for viibe.app/invite/:code and viibe://invite/:code
    - Read `code` from `useLocalSearchParams()`
    - On mount: call `storePendingReferral(code)` from useReferral hook
    - Display a full-screen welcome view matching the Emergent luxury aesthetic (dark bg #08070F, gold accent #C9A84C):
      - Title: "You've been invited to VIIBE"
      - Subtitle: "The live scene intelligence for Lagos"
      - CTA button: "Create Account" → navigates to the signup/onboarding screen (use `router.push('/(public)/onboarding')` or whichever auth entry route exists in the app)
      - If user is already authenticated, show "Open App" button → `router.replace('/(admin)')` or the main feed
    - The screen does NOT redeem the code itself — redemption happens after signup via useReferral hook

    Redemption call site: In the _layout.tsx or auth completion handler, call `redeemPendingReferral(token)` after a user completes signup (first authenticated session). Look for where auth state transitions from unauthenticated to authenticated for the first time — add the redemption call there. If no clear "first login" signal exists, call it on every auth restoration but the hook's AsyncStorage + 400-guard ensures idempotency.
  </action>
  <verify>
    <automated>cd c:/VIIBE/VibeApp/frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "invite|useReferral" | head -20; echo "TypeScript check done"</automated>
  </verify>
  <done>invite/[code].tsx renders without TS errors. useReferral.ts exports storePendingReferral, redeemPendingReferral, getPendingReferral. Deep link route exists in file system.</done>
</task>

<task type="auto">
  <name>Task 3: Add Invite button to profile screen</name>
  <files>frontend/app/(public)/profile.tsx</files>
  <action>
    In frontend/app/(public)/profile.tsx, add an Invite button to the profile screen.

    Behavior:
    1. On tap, call GET /referrals/my-code with the user's auth token
    2. Receive `{ code, link }` where link = "https://viibe.app/invite/{code}"
    3. Construct WhatsApp share URL: `https://wa.me/?text=I'm on VIIBE — the live scene intelligence for Lagos. Join me: ${link}`
    4. Call `Linking.openURL(whatsAppUrl)` — this opens WhatsApp with the message pre-filled; user taps Send. That is the "two taps" flow.
    5. Also show a "Copy link" fallback that calls `Clipboard.setString(link)` and shows a brief "Copied!" toast (use a simple 1.5s opacity animation on a Text element — no external toast library)

    UI placement: Add an "Invite Friends" row in the profile actions section, styled consistently with existing profile action items. Use a person-plus icon or similar from the existing icon set in the codebase.

    Loading state: While fetching the code, show a spinner in place of the button label. Error state: show "Try again" text if fetch fails.

    Do NOT restructure the profile screen — add the invite button inline with existing UI patterns.
  </action>
  <verify>
    <automated>cd c:/VIIBE/VibeApp/frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep "profile" | grep -i error | head -10; echo "Profile TS check done"</automated>
  </verify>
  <done>Profile screen compiles cleanly. The Invite button fetches the referral link and opens WhatsApp. Copy fallback works.</done>
</task>

</tasks>

<verification>
- app.json contains associatedDomains (iOS) and intentFilters (Android) for viibe.app/invite
- frontend/app/invite/[code].tsx exists and is reachable as a route
- frontend/src/hooks/useReferral.ts exports all three functions
- Profile screen has Invite button that opens WhatsApp pre-filled with referral link
- TypeScript compiles without errors on touched files
</verification>

<success_criteria>
A scout taps "Invite Friends" on their profile, gets a referral link, and can share it to WhatsApp in two taps. Anyone who taps the link lands on the invite screen (in-app if installed, App Store/Play Store if not). After signup, the referral code is auto-redeemed awarding clout to both parties.
</success_criteria>

<output>
After completion, create `.planning/phases/04-growth-loops/04-02-SUMMARY.md`
</output>
