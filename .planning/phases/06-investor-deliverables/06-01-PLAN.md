---
phase: 06-investor-deliverables
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/screens/WarRoomScreen.tsx
  - frontend/src/data/warRoomDemoData.ts
  - frontend/app/(public)/warroom.tsx
autonomous: true
requirements:
  - INVT-01

must_haves:
  truths:
    - "War Room opens as a full-screen view with no navigation chrome obscuring content"
    - "All active Lagos venues appear sorted by live score descending"
    - "Each venue row animates its score with a pulsing/counting animation"
    - "A scout activity feed shows recent check-ins/ratings scrolling below the venue list"
    - "A city-wide collective charge bar fills based on aggregate venue scores"
    - "The screen looks screenshot-ready at any time using demo data (no empty states, no loading spinners)"
  artifacts:
    - path: "frontend/src/screens/WarRoomScreen.tsx"
      provides: "Full War Room UI — venue list, score animations, scout feed, charge bar"
      min_lines: 200
    - path: "frontend/src/data/warRoomDemoData.ts"
      provides: "Static demo dataset — 12+ Lagos venues with scores, scout activity events"
      exports: ["WAR_ROOM_VENUES", "WAR_ROOM_SCOUT_FEED", "CITY_CHARGE"]
    - path: "frontend/app/(public)/warroom.tsx"
      provides: "Expo Router route entry point for the War Room screen"
  key_links:
    - from: "frontend/app/(public)/warroom.tsx"
      to: "frontend/src/screens/WarRoomScreen.tsx"
      via: "default import"
    - from: "frontend/src/screens/WarRoomScreen.tsx"
      to: "frontend/src/data/warRoomDemoData.ts"
      via: "named imports WAR_ROOM_VENUES, WAR_ROOM_SCOUT_FEED, CITY_CHARGE"
---

<objective>
Build the Detty December War Room — a full-screen React Native view that shows all active Lagos venues ranked by live vibe score, with animated score counters, a scrolling scout activity feed, and a city-wide collective charge bar. Uses static demo data so it looks screenshot-ready at any time.

Purpose: Primary visual investor artifact. A single screenshot of this screen communicates VIIBE's core value prop — real-time city energy — better than any slide.
Output: WarRoomScreen.tsx, demo data module, Expo Router entry point.
</objective>

<execution_context>
@C:/Users/OAJAGUN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OAJAGUN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/VIIBE/VibeApp/.planning/ROADMAP.md
@C:/VIIBE/VibeApp/.planning/STATE.md

Relevant source files to understand existing patterns:
- frontend/src/components/CityPulseBar.tsx — city heartbeat bar (reuse styling patterns)
- frontend/src/components/ActivityTicker.tsx — scrolling activity feed pattern
- frontend/src/store/types.ts — Venue interface (current_vibe_score, name, location fields)
- frontend/app/(public)/trending.tsx — example public screen structure

<interfaces>
From frontend/src/store/types.ts:
```typescript
export interface Venue {
  _id: string;
  name: string;
  location: string;
  current_vibe_score: number;
  // ...other fields
}

export interface CityPulseData {
  // city-level aggregate data
}
```

Key design tokens (from Emergent luxury aesthetic memory):
- Background: #0A0A0A (near-black)
- Accent: #FF3B30 (VIIBE red) or gold #D4AF37 for premium
- Card surface: #1A1A1A
- Text primary: #FFFFFF
- Text secondary: #888888
- Font: Use existing project font stack
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create War Room demo data module</name>
  <files>frontend/src/data/warRoomDemoData.ts</files>
  <action>
Create a static demo data module exporting three constants:

1. WAR_ROOM_VENUES: array of 12 Lagos venues, each with:
   - id (string), name (string), area (string, e.g. "Victoria Island", "Lekki", "Ikeja")
   - score (number, 40–97), trend ("up" | "down" | "stable")
   - ratingCount (number, 8–120), scoutCount (number, 2–30)
   - category ("club" | "bar" | "lounge" | "event")
   - isHot (boolean — true if score > 80)
   Real Lagos venue-style names: Quilox, Elevation, The Lekki Club, Hard Rock Cafe Lagos, Zinc Bar, Sky Bar, Chronicles, DNA Lounge, Club 57, Rumours, Rehab Beach, Zen Gardens
   Scores distributed so the list shows clear ranking contrast (top: 94, 89, 83..., bottom: 41, 38)

2. WAR_ROOM_SCOUT_FEED: array of 20 scout activity events, each with:
   - id, scoutHandle (e.g. "@viibe_scout_ayo"), action ("rated" | "checked in" | "flagged surge"), venueName, score (optional), timeAgo (e.g. "2m ago", "5m ago")
   Events should feel live — mix of handles, venues, and recent timestamps

3. CITY_CHARGE: object with:
   - percentage (number 0–100): aggregate city charge level — use 73
   - activeVenues (number): 12
   - activeScouts (number): 47
   - label (string): "Lagos is CHARGED"
  </action>
  <verify>
    <automated>cd C:/VIIBE/VibeApp && npx tsc --noEmit frontend/src/data/warRoomDemoData.ts 2>/dev/null || echo "check manually — tsc may need full project context"</automated>
  </verify>
  <done>File exists, exports WAR_ROOM_VENUES (12 items), WAR_ROOM_SCOUT_FEED (20 items), CITY_CHARGE object. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build WarRoomScreen and Expo Router entry</name>
  <files>frontend/src/screens/WarRoomScreen.tsx, frontend/app/(public)/warroom.tsx</files>
  <action>
Create WarRoomScreen.tsx as a full-screen React Native component. Layout from top to bottom:

HEADER (fixed):
- Title: "WAR ROOM" in bold caps, VIIBE red (#FF3B30), with a live pulse dot (animated opacity 1→0.3→1 on 1s loop using Animated API)
- Subtitle: "DETTY DECEMBER · LAGOS" in secondary text
- No back button — this is a full-screen showcase view

CITY CHARGE BAR (below header, full width):
- Label: city charge percentage + "CITY CHARGED" text
- Animated fill bar: width interpolated from 0 to percentage on mount (600ms ease-in-out)
- Show activeVenues + activeScouts counts inline
- Color: gradient from #FF3B30 to #FF8C00 (use a solid color if LinearGradient not already installed)

VENUE LIST (scrollable, takes ~60% of height):
- Section header: "LIVE VENUES — SORTED BY SCORE"
- Each venue row (VenueRow): score badge (large number, color-coded: ≥80 red, 60-79 orange, <60 grey), venue name (bold white), area label (secondary), trend arrow (↑↓—), rating count small text
- Rows separated by a 1px #2A2A2A border
- Top venue row has a subtle gold left border (#D4AF37) to indicate #1

SCOUT FEED (bottom ~35%, fixed height with internal ScrollView, auto-scrolling):
- Section header: "SCOUT ACTIVITY"
- Each feed item: scout handle (@xxx) in VIIBE red, action text in white, venue name in secondary, timeAgo right-aligned
- Auto-scroll: use setInterval + scrollRef.scrollTo every 3 seconds to advance by one item height (40px). Pause if user interacts.
- Max visible: 5 items at a time

Styling: All backgrounds #0A0A0A / #1A1A1A. Use StyleSheet.create. No external style libraries beyond what's already in the project.

Use React Native's built-in Animated API only (no Reanimated — avoids worklets complexity per project rules).

Create frontend/app/(public)/warroom.tsx as a simple entry point:
```tsx
import WarRoomScreen from '../../src/screens/WarRoomScreen';
export default WarRoomScreen;
```
  </action>
  <verify>
    <automated>ls C:/VIIBE/VibeApp/frontend/src/screens/WarRoomScreen.tsx && ls C:/VIIBE/VibeApp/frontend/app/(public)/warroom.tsx</automated>
  </verify>
  <done>Both files exist. WarRoomScreen renders header, charge bar, venue list, and scout feed. No import errors. Navigable via /warroom route in Expo Router.</done>
</task>

</tasks>

<verification>
1. `ls frontend/src/screens/WarRoomScreen.tsx frontend/src/data/warRoomDemoData.ts frontend/app/(public)/warroom.tsx` — all three files exist
2. No TypeScript errors on the new files (run `npx tsc --noEmit` in frontend/)
3. WarRoomScreen has: animated pulse dot in header, city charge bar with fill animation, venue list sorted by score, scout feed with auto-scroll
4. Demo data has 12 venues with realistic Lagos names and score distribution 38–94
</verification>

<success_criteria>
A screenshot of the War Room at any time shows: city charge bar at 73%, 12 Lagos venues ranked by score with the top venue gold-bordered, a scrolling scout activity feed at the bottom — no empty states, no loading spinners. Investor-ready visual.
</success_criteria>

<output>
After completion, create `.planning/phases/06-investor-deliverables/06-01-SUMMARY.md`
</output>
