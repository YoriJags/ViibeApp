---
phase: 01-billing-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .planning/phases/01-billing-foundation/01-02-GAPS.md
autonomous: true
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
  - BILL-04

must_haves:
  truths:
    - "Every BILL-XX requirement has a clear verdict: ✓ Implemented / ⚠ Partial / ✗ Missing"
    - "Each verdict cites at least one specific file path and function name as evidence"
    - "Every gap (⚠ or ✗) has a gap description that explains precisely what is absent or incorrect"
    - "The document records the kobo price mismatch finding if DEFAULT_PRICE_KOBO does not equal 200000"
    - "Phase 1 success criteria items 1–5 (from ROADMAP.md) each have a corresponding audit finding"
    - "The document contains a Resilience Findings section covering Naira fluctuation handling and network failure handling"
  artifacts:
    - path: ".planning/phases/01-billing-foundation/01-02-GAPS.md"
      provides: "Requirement-by-requirement gap analysis with verdicts, evidence, and resilience findings"
      min_lines: 70
  key_links:
    - from: ".planning/phases/01-billing-foundation/01-02-GAPS.md"
      to: ".planning/phases/01-billing-foundation/01-01-IMPL.md"
      via: "uses implementation inventory as source of truth"
    - from: ".planning/phases/01-billing-foundation/01-02-GAPS.md"
      to: ".planning/REQUIREMENTS.md"
      via: "maps each finding to a BILL-XX requirement ID"
---

<objective>
Audit the existing billing implementation against each BILL-01 through BILL-04 requirement and produce a gap analysis document with verdicts, evidence, and resilience findings.

Purpose: The user needs to know exactly what is implemented, what is partial, and what is missing — with line-level evidence — so they can prioritise and scope the actual implementation work in a future phase. The resilience audit (Naira fluctuation + network failures) is required by CONTEXT.md.
Output: `.planning/phases/01-billing-foundation/01-02-GAPS.md` — a structured gap analysis with per-requirement verdicts, evidence citations, resilience findings, and a prioritised gap list.
</objective>

<execution_context>
@C:/Users/OAJAGUN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OAJAGUN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-billing-foundation/01-CONTEXT.md
@.planning/phases/01-billing-foundation/01-RESEARCH.md
@.planning/phases/01-billing-foundation/01-01-IMPL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit BILL-01 and BILL-02 against implementation</name>
  <files>.planning/phases/01-billing-foundation/01-02-GAPS.md</files>
  <action>
Using `.planning/phases/01-billing-foundation/01-01-IMPL.md` as source material (supplemented by re-reading specific source files if a detail is ambiguous), audit BILL-01 and BILL-02.

**BILL-01 definition:** Scout can subscribe to Vibe+ via RevenueCat (Apple IAP on iOS, Google Play Billing on Android, Paystack on web) for ₦2,000/month; Oracle AI + Night Planner unlock immediately on subscription.

Audit checklist for BILL-01:
- [ ] Paystack web subscribe route exists (POST /api/subscription/initialize)
- [ ] Price constant equals 200,000 kobo (₦2,000) — NOT 150,000 (₦1,500)
- [ ] Oracle premium route gated behind _check_vibe_plus()
- [ ] Night Planner route (POST /api/planner/chat) gated behind _check_vibe_plus()
- [ ] RevenueCat SDK present in frontend/package.json
- [ ] RevenueCat webhook endpoint registered on backend

**BILL-02 definition:** New Vibe+ subscriber gets 7-day free trial before first charge; subscription status screen shows trial end date, feature list, and cancel link.

Audit checklist for BILL-02:
- [ ] trial_ends_at field stored on subscription initialize (DB or payload)
- [ ] Paystack start_date used to delay first charge by 7 days
- [ ] GET /api/subscription/status returns trial_ends_at in response
- [ ] VibePlusModal.tsx or SubscriptionStatusScreen.tsx renders trial end date
- [ ] Feature list shown on subscription status
- [ ] Cancel link present on subscription status UI

Create `.planning/phases/01-billing-foundation/01-02-GAPS.md` with the following structure for BILL-01 and BILL-02:

```
# Phase 1 — Billing Gap Analysis
*Audited: {date}*
*Source: 01-01-IMPL.md + direct code reads*

## Audit Methodology
Requirements compared against 01-01-IMPL.md and direct re-reads of source files.
Verdict scale:
- ✓ Implemented — fully satisfies the requirement
- ⚠ Partial — some aspects implemented, specific gaps noted
- ✗ Missing — requirement is not implemented

---

## BILL-01: Vibe+ Subscribe + Feature Gates

**Requirement:** Scout can subscribe to Vibe+ via RevenueCat (Apple IAP on iOS, Google Play Billing on Android, Paystack web) for ₦2,000/month; Oracle AI + Night Planner unlock immediately.

**Verdict:** [✓ / ⚠ / ✗]

### Evidence

| Sub-requirement | Status | Evidence (file:line or description) |
|-----------------|--------|--------------------------------------|
| Paystack subscribe route (web) | [✓/✗] | [file path + function name] |
| Price = ₦2,000 (200,000 kobo) | [✓/✗] | [exact constant value found] |
| Oracle Premium gated | [✓/✗] | [file path + gate call] |
| Night Planner gated | [✓/✗] | [file path + gate call or "absent"] |
| RevenueCat iOS/Android SDK | [✓/✗] | [package.json status] |
| RevenueCat webhook endpoint | [✓/✗] | [backend route status] |

### Gaps (if any)
- [Gap 1: precise description of what is missing or wrong]
- [Gap 2: ...]

---

## BILL-02: 7-Day Free Trial

**Requirement:** New Vibe+ subscriber sees 7-day free trial before first charge; subscription status screen shows trial end date, feature list, cancel link.

**Verdict:** [✓ / ⚠ / ✗]

### Evidence

| Sub-requirement | Status | Evidence |
|-----------------|--------|----------|
| trial_ends_at stored on initialize | [✓/✗] | [file path or "absent"] |
| Paystack start_date used for delay | [✓/✗] | [file path or "absent"] |
| Status endpoint returns trial_ends_at | [✓/✗] | [endpoint + field name or "absent"] |
| Trial end date shown in UI | [✓/✗] | [component name or "absent"] |
| Feature list on status UI | [✓/✗] | [component name or "absent"] |
| Cancel link on status UI | [✓/✗] | [component name or "absent"] |

### Gaps (if any)
- [...]
```

Do NOT suggest fixes. Only record observations and gaps.
  </action>
  <verify>
    <automated>ls "C:/Users/OAJAGUN/OneDrive/Documents/Code/VibeApp/.planning/phases/01-billing-foundation/01-02-GAPS.md" && echo "file exists"</automated>
  </verify>
  <done>
`01-02-GAPS.md` exists and contains BILL-01 and BILL-02 sections, each with a verdict, an evidence table, and a gaps list. Each evidence table row cites a specific file path or notes "absent".
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit BILL-03 and BILL-04, resilience checks, success criteria, and priority list</name>
  <files>.planning/phases/01-billing-foundation/01-02-GAPS.md</files>
  <action>
Continue the gap analysis in `01-02-GAPS.md`. Using `01-01-IMPL.md` as source (supplemented by direct re-reads as needed), audit BILL-03 and BILL-04, run the required resilience checklists, then produce a phase-level gap summary.

**BILL-03 definition:** Merchant SaaS subscription via Paystack at ₦150,000/month with auto-renewing billing; survives browser refresh and backend restart.

Audit checklist for BILL-03:
- [ ] Dedicated merchant SaaS subscribe route exists (e.g., POST /api/merchant/saas/subscribe)
- [ ] merchant_subscriptions MongoDB collection used (not merchant_wallets)
- [ ] Paystack Plan used (not one-off transaction — must include plan: plan_code in initialize)
- [ ] Amount = ₦150,000 = 15,000,000 kobo
- [ ] Webhook handles subscription.create event (stores subscription_code + email_token)
- [ ] Webhook handles invoice.payment_failed event
- [ ] Webhook handles subscription.disable event
- [ ] Subscription status route returns active/inactive reliably (survives restart because stored in DB)

**BILL-04 definition:** Merchant can buy Pulse Drop boosts only after active SaaS subscription; blocked with upsell prompt if no subscription.

Audit checklist for BILL-04:
- [ ] pulse_drops.py purchase route checks for active merchant SaaS subscription before wallet deduction
- [ ] Returns 403 (or equivalent) with descriptive error when SaaS not active
- [ ] Frontend PulseDropSelector.tsx shows upsell prompt when blocked
- [ ] Webhook signature verification still in place (fake charge.success with wrong sig returns 401)

Also audit **Phase 1 Success Criteria** from ROADMAP.md (5 items) — each maps to at least one BILL-XX:

Success Criterion 1: Scout subscribes to Vibe+ ₦2,000/month, Oracle AI + Night Planner immediately accessible → maps to BILL-01
Success Criterion 2: Vibe+ subscriber sees 7-day free trial, status screen shows trial end date + feature list + cancel link → maps to BILL-02
Success Criterion 3: Merchant subscribes to SaaS ₦150,000/month, subscription survives refresh + restart → maps to BILL-03
Success Criterion 4: Merchant buys Pulse Drop only after active SaaS; blocked with upsell if no SaaS → maps to BILL-04
Success Criterion 5: Paystack webhook verifies HMAC-SHA512; fake charge.success with wrong signature returns 401, grants no access → maps to security of BILL-01+BILL-03

**REQUIRED: Naira fluctuation checklist** — Re-read `backend/app/routes/subscriptions.py` and `backend/app/routes/webhooks.py` (and any Flutterwave file if present) and check:
- [ ] Does the webhook verify that the received `amount` field matches the expected kobo amount? (e.g., does it compare `data['amount']` against `DEFAULT_PRICE_KOBO`?) Record the exact check if present, or "absent — no amount comparison" if not.
- [ ] Are kobo amounts stored at charge-time in the DB record (snapshot), or is the subscription record amount-agnostic and the price re-computed on verify from the current constant? Record which pattern is used.
- [ ] Are price constants (DEFAULT_PRICE_KOBO, merchant SaaS amount) hardcoded magic numbers, or referenced from a single config source? If hardcoded in multiple places, record each location — this is a silent-breakage risk if pricing changes.

**REQUIRED: Failed network request checklist** — Re-read `backend/app/routes/subscriptions.py` and any Flutterwave service file, focusing on the outbound API call to Paystack/Flutterwave (the httpx or requests call that initialises or verifies a transaction), and check:
- [ ] Does the httpx/requests call include a `timeout=` parameter? Record the timeout value if present, or "absent — no timeout" if not.
- [ ] Is the outbound API call wrapped in a `try/except` block that catches `httpx.RequestError`, `httpx.TimeoutException`, or `requests.exceptions.RequestException`? Record what is caught and what HTTP response the route returns on failure (e.g., 503, 500, or "unhandled — exception propagates").
- [ ] If the provider is unreachable, what does the caller (frontend) receive? Record the status code and body, or "unknown — exception not caught" if unhandled.

Append to `01-02-GAPS.md`:

```
---

## BILL-03: Merchant SaaS Subscription

**Requirement:** Merchant can subscribe to SaaS dashboard tier via Paystack at ₦150,000/month with auto-renewing billing; survives browser refresh + backend restart.

**Verdict:** [✓ / ⚠ / ✗]

### Evidence

| Sub-requirement | Status | Evidence |
|-----------------|--------|----------|
| Merchant SaaS subscribe route | [✓/✗] | [file path or "absent"] |
| Uses Paystack Plan (auto-renew) | [✓/✗] | [evidence or "absent"] |
| Amount = 15,000,000 kobo | [✓/✗] | [constant or "absent"] |
| merchant_subscriptions collection | [✓/✗] | [evidence or "absent"] |
| Webhook: subscription.create | [✓/✗] | [file path or "absent"] |
| Webhook: invoice.payment_failed | [✓/✗] | [file path or "absent"] |
| Webhook: subscription.disable | [✓/✗] | [file path or "absent"] |
| Subscription survives restart (DB-backed) | [✓/✗] | [evidence or "absent"] |

### Gaps (if any)
- [...]

---

## BILL-04: Pulse Drop SaaS Gate

**Requirement:** Merchant can buy Pulse Drop boosts only after active SaaS subscription; blocked with upsell prompt if no subscription.

**Verdict:** [✓ / ⚠ / ✗]

### Evidence

| Sub-requirement | Status | Evidence |
|-----------------|--------|----------|
| Pulse Drop route checks SaaS subscription | [✓/✗] | [file path or "absent"] |
| Returns 403 without active SaaS | [✓/✗] | [status code in code or "absent"] |
| Frontend upsell prompt on block | [✓/✗] | [component name or "absent"] |
| Webhook signature verification | [✓/✗] | [payments.py function call or "absent"] |

### Gaps (if any)
- [...]

---

## Phase 1 Success Criteria Check

| SC# | Description | BILL-XX | Achievable with current code? |
|-----|-------------|---------|-------------------------------|
| SC-1 | Vibe+ ₦2,000/month, Oracle + Planner unlock | BILL-01 | [Yes / No — reason] |
| SC-2 | 7-day trial, status screen | BILL-02 | [Yes / No — reason] |
| SC-3 | Merchant SaaS ₦150,000/month, auto-renew, persists | BILL-03 | [Yes / No — reason] |
| SC-4 | Pulse Drop gated by SaaS, upsell if blocked | BILL-04 | [Yes / No — reason] |
| SC-5 | Webhook HMAC-SHA512, fake sig → 401, no access | BILL-01/03 | [Yes / No — reason] |

---

## Resilience Findings

### Naira Fluctuation Handling

| Check | Finding |
|-------|---------|
| Webhook verifies received amount == expected amount | [Yes — describe check / No — absent] |
| Kobo amount stored at charge-time (snapshot) vs. re-computed from constant | [Snapshot / Re-computed from constant / Unknown] |
| Price constants: single config source vs. hardcoded in multiple places | [Single source (config.py) / Hardcoded in N places: list each file:line] |

**Naira fluctuation gaps:**
- [Describe any gaps, e.g. "Webhook does not verify received amount — a Paystack replay with a lower amount would activate a subscription" / "No gaps found"]

### Network Failure Handling

| Check | Finding |
|-------|---------|
| httpx/requests timeout parameter on provider API call | [Present: timeout={value}s / Absent — no timeout] |
| try/except around provider API call | [Present: catches {exception types}, returns HTTP {status} / Absent — exception propagates] |
| HTTP response caller receives if provider unreachable | [{status code + body description} / Unknown — unhandled] |

**Network failure gaps:**
- [Describe any gaps, e.g. "No timeout on Paystack initialize call — hangs indefinitely if Paystack is down" / "No gaps found"]

---

## Prioritised Gap List

Gaps ordered by Phase 1 success criteria impact (highest impact first).

### P1 — Blockers (Phase 1 success criteria cannot be met without fixing these)
- [ ] [Gap description] — affects SC-{N}, requirement BILL-{XX}
- [ ] ...

### P2 — Important (requirement partially met; product works but incorrectly)
- [ ] [Gap description] — affects BILL-{XX}
- [ ] ...

### P3 — Minor / Deferred (noted for future phases)
- [ ] [Gap description] — deferred per CONTEXT.md
- [ ] ...

---

## Recommendation for Next Steps

[2-4 sentences: What is the overall implementation status? Which P1 gaps must be fixed before Phase 1 can be considered complete? Which resilience gaps are minor enough to fix in-phase (try/except, timeout parameters) vs. requiring a future phase?]

Note: Per CONTEXT.md, minor gap filling (error handling, logging, defensive checks) is IN SCOPE for this phase. Major refactors and new features are OUT OF SCOPE.
```

Do NOT fix or implement any gap. This is a documentation task only.
  </action>
  <verify>
    <automated>grep -l "Resilience Findings" "C:/Users/OAJAGUN/OneDrive/Documents/Code/VibeApp/.planning/phases/01-billing-foundation/01-02-GAPS.md" && echo "section present"</automated>
  </verify>
  <done>
`01-02-GAPS.md` contains BILL-03 and BILL-04 sections (each with verdict + evidence table + gaps list), a Phase 1 Success Criteria Check table, a Resilience Findings section with both Naira fluctuation and Network failure sub-sections (each with a findings table and gaps list), a Prioritised Gap List with P1/P2/P3 categories, and a Recommendation section. Every ✗ or ⚠ verdict in the document has at least one corresponding entry in the gap list.
  </done>
</task>

</tasks>

<verification>
- `01-02-GAPS.md` exists and is non-empty
- All four BILL-XX sections present (BILL-01 through BILL-04)
- Each section has: Verdict line, Evidence table with file citations, Gaps list
- Phase 1 Success Criteria Check table covers all 5 SC items
- Resilience Findings section present with both Naira fluctuation and Network failure sub-sections
- Each resilience sub-section has a findings table and a gaps list
- Prioritised Gap List is populated (not empty) unless all requirements are ✓ Implemented
- No implementation code appears anywhere in the document
- No task items ("TODO: fix X") appear — only observations
</verification>

<success_criteria>
`01-02-GAPS.md` gives the user a complete, evidence-backed picture of the billing implementation status including resilience posture. They can read the Prioritised Gap List and Resilience Findings sections and immediately know what must be fixed for Phase 1 to meet its success criteria, with direct file references for each gap. No source file needs to be opened to understand the findings.
</success_criteria>

<output>
After completion, create `.planning/phases/01-billing-foundation/01-02-SUMMARY.md` using the standard summary template.
</output>
