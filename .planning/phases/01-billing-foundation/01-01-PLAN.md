---
phase: 01-billing-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-billing-foundation/01-01-IMPL.md
autonomous: true
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
  - BILL-04

must_haves:
  truths:
    - "A markdown document exists that lists every billing-related file with its absolute path, its role in the billing system, and the key exports/functions it provides"
    - "The document accurately describes how the Paystack Vibe+ consumer subscription flow works end to end (initialize → verify → webhook → DB state)"
    - "The document records the exact price constants used in the codebase today (kobo values)"
    - "The document identifies which backend routes are gated behind subscription checks and which are not"
    - "The document records the current state of RevenueCat integration (present or absent)"
    - "The document covers Flutterwave integration: whether a Flutterwave file exists, its role, and when it is used vs. Paystack"
  artifacts:
    - path: ".planning/phases/01-billing-foundation/01-01-IMPL.md"
      provides: "Complete inventory of existing billing implementation (Paystack + Flutterwave)"
      min_lines: 80
  key_links:
    - from: ".planning/phases/01-billing-foundation/01-01-IMPL.md"
      to: "backend/app/routes/subscriptions.py"
      via: "direct code read"
    - from: ".planning/phases/01-billing-foundation/01-01-IMPL.md"
      to: "backend/app/routes/webhooks.py"
      via: "direct code read"
---

<objective>
Read and document every billing-related file in the existing codebase to produce a complete implementation inventory.

Purpose: The billing system was implemented manually. Before auditing it against requirements, we need an accurate map of what was built — exact file paths, functions, constants, and data flow — so the gap analysis in Plan 02 has reliable source material. Coverage includes both Paystack and Flutterwave paths.
Output: `.planning/phases/01-billing-foundation/01-01-IMPL.md` — a structured inventory document covering all backend and frontend billing files for both payment providers.
</objective>

<execution_context>
@C:/Users/OAJAGUN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OAJAGUN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-billing-foundation/01-CONTEXT.md
@.planning/phases/01-billing-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read and document backend billing files</name>
  <files>.planning/phases/01-billing-foundation/01-01-IMPL.md</files>
  <action>
First, discover all Flutterwave files in the backend. Run the following searches before reading any files:

```
glob: backend/app/services/*flutter*
glob: backend/app/routes/*flutter*
grep "flutterwave" backend/app/services/ (case-insensitive, all .py files)
grep "flutterwave" backend/app/routes/ (case-insensitive, all .py files)
```

Record the result: either the specific file paths found, or "no Flutterwave-specific files found" if the searches return nothing.

Then read each of the following backend files in full:

1. `backend/app/services/payments.py`
2. Any Flutterwave service file discovered above (e.g., `backend/app/services/flutterwave.py`) — skip if absent
3. `backend/app/routes/subscriptions.py`
4. `backend/app/routes/webhooks.py`
5. `backend/app/routes/planner.py`
6. `backend/app/routes/pulse_drops.py`
7. `backend/app/routes/oracle.py`
8. `backend/app/config.py` (billing-relevant sections only: price constants, PULSE_DROP_TIERS)
9. `backend/api/index.py` (scan for which subscription/webhook/pulse_drop/planner routes are registered)

For each file, record:
- Absolute file path
- Purpose (1-2 sentences)
- Key exports / functions / route handlers
- Billing-relevant constants (kobo values, plan names, reference prefixes)
- Any subscription gate checks present or absent

Then create `.planning/phases/01-billing-foundation/01-01-IMPL.md` with the following structure:

```
# Phase 1 — Billing Implementation Inventory
*Recorded: {date}*

## Overview
[2-3 sentence summary of what billing infrastructure currently exists, naming both providers in use]

## Backend Files

### backend/app/services/payments.py
**Role:** ...
**Key functions:**
- `verify_paystack_signature(payload, signature) -> bool` — ...
**Constants:** ...
**Notes:** ...

### backend/app/routes/subscriptions.py
**Role:** ...
**Key functions/routes:**
- `POST /api/subscription/initialize` — ...
- `POST /api/subscription/verify/{reference}` — ...
- `GET /api/subscription/status` — ...
- `_check_vibe_plus(user) -> bool` — ...
- `_apply_subscription(user, reference) -> None` — ...
**Price constant:** DEFAULT_PRICE_KOBO = {value} (= ₦{naira})
**Trial support:** [yes/no — describe]
**Notes:** ...

### backend/app/routes/webhooks.py
**Role:** ...
**Registered events handled:**
- `charge.success` — [routing logic: which reference prefixes, what actions]
- [any others]
**Signature verification:** [describe]
**Notes:** ...

### backend/app/routes/planner.py
**Role:** ...
**Route:** POST /api/planner/chat
**Vibe+ gate:** [present/absent — exact evidence]
**Notes:** ...

### backend/app/routes/pulse_drops.py
**Role:** ...
**Route:** POST /api/pulse-drops/purchase (or actual route path)
**SaaS subscription gate:** [present/absent — exact evidence]
**Notes:** ...

### backend/app/routes/oracle.py
**Role:** ...
**Gated route:** GET /venues/{venue_id}/oracle/premium
**Vibe+ gate:** [present — describe call to _check_vibe_plus]
**Notes:** ...

### backend/app/config.py (billing constants)
**Price constants:**
- DEFAULT_PRICE_KOBO = {value}
- PULSE_DROP_TIERS = {full value}
- [any others]

### backend/api/index.py (Vercel entry point — route registration)
**Billing routes registered:** [list which of the above are present]
**Missing from Vercel entry:** [list any that are in server.py but absent here]

## Flutterwave Integration

**Discovery result:** [list file paths found by glob/grep above, or "no Flutterwave-specific files found"]

### {flutterwave file path, e.g. backend/app/services/flutterwave.py} [or "Absent — no dedicated file"]
**Role:** [describe purpose, or "N/A"]
**Key functions:** [list, or "N/A"]
**When used vs. Paystack:** [describe routing logic — e.g., "used for mobile payments when provider=flutterwave param passed", or "not wired to any route", or "absent"]
**Webhook handler present:** [yes — describe / no]
**Notes:** ...

[If Flutterwave logic is embedded in payments.py or webhooks.py rather than a separate file, describe it here instead:]
**Embedded Flutterwave logic in payments.py:** [describe any FW-specific functions found, or "none"]
**Embedded Flutterwave logic in webhooks.py:** [describe any FW event handlers found, or "none"]

## Merchant SaaS
**Dedicated route file:** [exists/absent]
**merchant_subscriptions collection:** [present/absent in codebase]
**Webhook handling for subscription.create / invoice.payment_failed / subscription.disable:** [present/absent]

## Frontend Files (to be read in Task 2)
[placeholder — completed in Task 2]

## RevenueCat Integration Status
[placeholder — completed in Task 2]
```

Do NOT fix or suggest fixes. Record only what is observed in the code.
  </action>
  <verify>
    <automated>ls C:/Users/OAJAGUN/OneDrive/Documents/Code/VibeApp/.planning/phases/01-billing-foundation/01-01-IMPL.md && echo "file exists"</automated>
  </verify>
  <done>
`01-01-IMPL.md` exists and contains documented sections for all backend files listed above plus the Flutterwave Integration section. The Flutterwave section records either the specific file found (with role, functions, and webhook handler status) or explicitly states "no Flutterwave-specific files found" with embedded logic noted if any. Each other section records the file's role, key functions, and any billing-gate status (present or absent).
  </done>
</task>

<task type="auto">
  <name>Task 2: Read and document frontend billing files, complete inventory</name>
  <files>.planning/phases/01-billing-foundation/01-01-IMPL.md</files>
  <action>
Read each of the following frontend files in full:

1. `frontend/src/components/VibePlusModal.tsx`
2. `frontend/src/components/NightPlannerModal.tsx`
3. `frontend/src/components/PulseDropSelector.tsx`
4. `frontend/src/store/vibeStore.ts` (billing-relevant sections: isVibePlus, initializeVibePlus, verifyVibePlus, refreshSubscriptionStatus)

Also check these paths for merchant subscription UI (record absent if not found):
5. `frontend/app/merchant/subscription.tsx` (or any subscription screen in the merchant directory)
6. Any component named `MerchantSaasModal.tsx` or `SubscriptionStatusScreen.tsx` in `frontend/src/components/`

For each file, record:
- Absolute file path
- Purpose
- Key functions / state / UI states shown
- Price strings hardcoded in UI (exact text)
- Any Vibe+ gate checks present (calls to isVibePlus() before showing content)
- Any merchant SaaS gate present

Also check whether `react-native-purchases` appears in `frontend/package.json` (RevenueCat SDK presence check).

Append the following sections to `.planning/phases/01-billing-foundation/01-01-IMPL.md`:

```
## Frontend Files

### frontend/src/components/VibePlusModal.tsx
**Role:** ...
**UI states:** [list states: paywall, processing, awaiting_verify, success, error — confirm what exists]
**Price displayed:** "{exact hardcoded price string}"
**Trial UI:** [present/absent]
**Paystack open method:** [expo-web-browser or other]
**Notes:** ...

### frontend/src/components/NightPlannerModal.tsx
**Role:** ...
**Vibe+ gate:** [present/absent — describe any isVibePlus() check]
**Notes:** ...

### frontend/src/components/PulseDropSelector.tsx
**Role:** ...
**Merchant SaaS gate:** [present/absent]
**Notes:** ...

### frontend/src/store/vibeStore.ts (billing slice)
**Billing actions:**
- isVibePlus() — ...
- initializeVibePlus() — ...
- verifyVibePlus() — ...
- refreshSubscriptionStatus() — ...
**Backend endpoints called:** [list URLs]
**Notes:** ...

### Merchant Subscription UI
**frontend/app/merchant/subscription.tsx:** [exists with description / absent]
**MerchantSaasModal.tsx:** [exists with description / absent]
**SubscriptionStatusScreen.tsx:** [exists with description / absent]

## RevenueCat Integration Status
**react-native-purchases in package.json:** [yes/no]
**RevenueCat API key in app.json or app.config.js:** [yes/no]
**Any RevenueCat imports in codebase:** [yes/no — grep result]
**Status:** [Zero integration / Partial / Complete]

## Summary Table

| Component | File | Status |
|-----------|------|--------|
| Paystack signature verify | backend/app/services/payments.py | Built |
| Flutterwave integration | backend/app/services/flutterwave.py (or embedded) | [Built / Absent] |
| Vibe+ initialize | backend/app/routes/subscriptions.py | Built |
| Vibe+ webhook handler | backend/app/routes/webhooks.py | Built |
| Oracle Vibe+ gate | backend/app/routes/oracle.py | Built |
| Night Planner Vibe+ gate | backend/app/routes/planner.py | [Built / Missing] |
| Merchant SaaS routes | backend/app/routes/merchant_subscriptions.py | [Built / Missing] |
| Merchant SaaS webhooks | backend/app/routes/webhooks.py | [Built / Missing] |
| Pulse Drop SaaS gate | backend/app/routes/pulse_drops.py | [Built / Missing] |
| Vibe+ paywall UI | frontend/src/components/VibePlusModal.tsx | Built |
| Night Planner gate UI | frontend/src/components/NightPlannerModal.tsx | [Built / Missing] |
| Merchant SaaS paywall UI | frontend/app/merchant/subscription.tsx | [Built / Missing] |
| RevenueCat SDK | frontend/package.json | [Present / Absent] |
```

Do NOT fix or suggest fixes. Record only what is observed.
  </action>
  <verify>
    <automated>grep -l "RevenueCat Integration Status" "C:/Users/OAJAGUN/OneDrive/Documents/Code/VibeApp/.planning/phases/01-billing-foundation/01-01-IMPL.md" && echo "section present"</automated>
  </verify>
  <done>
`01-01-IMPL.md` now contains all Frontend Files sections, a RevenueCat Integration Status section, and a completed Summary Table. The Summary Table includes a Flutterwave row with a definitive [Built / Absent] status. Every row in the Summary Table has a definitive status drawn from direct code observation.
  </done>
</task>

</tasks>

<verification>
- `01-01-IMPL.md` exists and is non-empty
- Document covers all backend files, all frontend billing files, and a Flutterwave Integration section
- Flutterwave section states either specific file(s) found or "no Flutterwave-specific files found", with embedded logic noted if applicable
- Summary Table present with definitive status for all 13 rows (including Flutterwave row)
- No implementation suggestions, code fixes, or task items appear in the document — it is purely observational
- Price constant values are recorded exactly (kobo integers, not approximations)
</verification>

<success_criteria>
`01-01-IMPL.md` is a self-contained, accurate inventory of the billing implementation as it exists in the codebase today, covering both Paystack and Flutterwave paths. A reader with no prior context can understand what was built, what files own what concerns, which payment provider handles which flows, and what the exact constants and route paths are — without needing to open any source file.
</success_criteria>

<output>
After completion, create `.planning/phases/01-billing-foundation/01-01-SUMMARY.md` using the standard summary template.
</output>
