---
phase: 01-billing-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - backend/app/services/payments.py
  - backend/app/routes/subscriptions.py
  - backend/app/routes/webhooks.py
autonomous: true
requirements:
  - BILL-01
  - BILL-03

must_haves:
  truths:
    - "Every payment provider API call (Paystack initialize, Paystack verify, any Flutterwave call) has a timeout parameter"
    - "Every payment provider API call is wrapped in try/except that catches network errors and returns a structured HTTP error response instead of crashing"
    - "Webhook signature verification failures are logged with enough context to diagnose replay attacks"
  artifacts:
    - path: "backend/app/routes/subscriptions.py"
      provides: "Hardened payment initialization and verification routes with timeout + try/except"
    - path: "backend/app/routes/webhooks.py"
      provides: "Webhook handler with logged signature verification failures"
    - path: "backend/app/services/payments.py"
      provides: "Payment service with defensive error handling"
  key_links:
    - from: "backend/app/routes/subscriptions.py"
      to: "backend/app/services/payments.py"
      via: "calls verify_paystack_signature and payment API functions"
    - from: ".planning/phases/01-billing-foundation/01-02-GAPS.md"
      to: "backend/app/routes/subscriptions.py"
      via: "gap analysis identifies specific missing try/except and timeout locations"

must_haves:
  truths:
    - "Every outbound payment provider API call has a timeout parameter (value: 10-15 seconds)"
    - "Every outbound payment provider API call has try/except that catches network/timeout errors and returns HTTP 503 with a JSON error body"
    - "Signature verification failures in webhooks.py are logged with the request IP and first 8 chars of the received signature (not the full secret)"
  artifacts:
    - path: "backend/app/routes/subscriptions.py"
      provides: "Subscription routes with timeout + network error handling on provider calls"
    - path: "backend/app/routes/webhooks.py"
      provides: "Webhook handler with failure logging"
    - path: "backend/app/services/payments.py"
      provides: "Payment helpers with defensive handling (if changes needed here)"
  key_links:
    - from: "backend/app/routes/subscriptions.py"
      to: "httpx client call"
      via: "timeout=15.0 parameter added"
    - from: "backend/app/routes/webhooks.py"
      to: "logger.warning"
      via: "called on signature mismatch with context fields"
---

<objective>
Apply minor defensive fixes identified by the gap analysis in Plan 02: add timeout parameters to payment provider API calls, wrap those calls in try/except for network failures, and add logging for webhook signature verification failures.

Purpose: CONTEXT.md explicitly allows minor gap filling (error handling, logging, small defensive checks). These fixes address the resilience gaps that Plan 02 documented without changing any business logic or data model.
Output: Updated backend files with hardened error handling on payment provider calls and improved webhook failure logging.
</objective>

<execution_context>
@C:/Users/OAJAGUN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OAJAGUN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-billing-foundation/01-CONTEXT.md
@.planning/phases/01-billing-foundation/01-02-GAPS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add timeout and try/except to payment provider API calls</name>
  <files>backend/app/routes/subscriptions.py, backend/app/services/payments.py</files>
  <action>
Read `01-02-GAPS.md` section "Network Failure Handling" first. The gaps listed there are the authoritative list of what to fix. Only fix items confirmed as gaps — do NOT add handling to calls that the gap analysis found already have it.

For each outbound httpx (or requests) call to Paystack or Flutterwave that the gap analysis flagged as missing a timeout:
- Add `timeout=15.0` to the call (15 seconds covers slow provider responses without hanging indefinitely)
- Example: `await client.post(url, json=payload)` → `await client.post(url, json=payload, timeout=15.0)`

For each outbound call flagged as missing try/except:
- Wrap the call in try/except catching `httpx.RequestError` and `httpx.TimeoutException` (or `requests.exceptions.RequestException` if using requests)
- On exception: log `logger.error("Payment provider unreachable: %s", str(e))` and return an appropriate HTTP error response with status 503 and body `{"error": "Payment provider temporarily unavailable. Please try again."}` — do NOT expose the raw exception message to the client
- Preserve existing happy-path logic exactly — only add the error branch

Do NOT change any business logic, DB writes, response schemas, or status codes on the success path. Do NOT refactor function structure. Minimal surgical additions only.

If `payments.py` has helper functions that make provider calls (identified in gap analysis as lacking try/except), apply the same pattern there. If `payments.py` is clean per gap analysis, leave it untouched.

Also check for any Flutterwave service file identified in `01-01-IMPL.md` — apply the same timeout + try/except pattern to its outbound calls if flagged in the gap analysis.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('C:/Users/OAJAGUN/OneDrive/Documents/Code/VibeApp/backend/app/routes/subscriptions.py').read()); print('syntax ok')"</automated>
  </verify>
  <done>
All outbound payment provider API calls flagged in `01-02-GAPS.md` Network Failure Handling section now have `timeout=15.0` and are wrapped in try/except returning HTTP 503 on network failure. `subscriptions.py` (and `payments.py` if applicable) parse without syntax errors. No business logic was changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add logging for webhook signature verification failures</name>
  <files>backend/app/routes/webhooks.py</files>
  <action>
Read `01-02-GAPS.md` section "Resilience Findings" to confirm what the gap analysis found about signature verification logging. Only proceed with the fix if the gap is confirmed there.

In `backend/app/routes/webhooks.py`, locate the point where `verify_paystack_signature` (or equivalent) is called and the result is False (signature mismatch).

Add a log statement at that point:

```python
logger.warning(
    "Webhook signature verification failed — possible replay attack or misconfigured secret",
    extra={
        "received_sig_prefix": received_signature[:8] if received_signature else "missing",
        "event_type": payload.get("event", "unknown"),
    }
)
```

Rules:
- Log level: `warning` (not `error` — a single failure may be a misconfigured client, not an attack)
- Do NOT log the full signature, the raw secret, or the full payload body
- Do NOT change the HTTP response returned on failure (keep existing 401 or equivalent)
- If a logger is already imported in the file, use it. If not, add `import logging` and `logger = logging.getLogger(__name__)` at the top of the file (after existing imports)
- Do NOT add logging anywhere else — only at the signature failure branch

If the gap analysis found that logging is already present on the failure path, skip this task and note "no change needed per gap analysis".
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('C:/Users/OAJAGUN/OneDrive/Documents/Code/VibeApp/backend/app/routes/webhooks.py').read()); print('syntax ok')"</automated>
  </verify>
  <done>
`webhooks.py` parses without syntax errors. The signature verification failure branch now logs a warning with `received_sig_prefix` and `event_type` fields. No response codes or business logic were changed.
  </done>
</task>

</tasks>

<verification>
- `subscriptions.py` has no syntax errors (ast.parse passes)
- `webhooks.py` has no syntax errors (ast.parse passes)
- Every gap listed in `01-02-GAPS.md` Network Failure Handling section is now addressed or explicitly noted as "already present per gap analysis"
- No business logic, DB write patterns, or success-path response schemas were changed
- No new dependencies introduced beyond what is already imported in each file
</verification>

<success_criteria>
The minor resilience gaps identified in Plan 02 are closed: payment provider API calls will time out after 15 seconds and return HTTP 503 instead of hanging or crashing, and signature verification failures are logged with enough context for incident diagnosis. All changes are surgical additions with no business logic modifications.
</success_criteria>

<output>
After completion, create `.planning/phases/01-billing-foundation/01-03-SUMMARY.md` using the standard summary template.
</output>
